rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the authenticated server using its IAM identity.
    // Admin SDK access uses IAM and is not granted by these client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
