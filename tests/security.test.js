import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readdirSync,readFileSync,existsSync} from 'node:fs';
test('公開配布ファイルに実キー・秘密鍵を含めない',()=>{for(const file of readdirSync(new URL('../dist/',import.meta.url))){const content=readFileSync(new URL('../dist/'+file,import.meta.url),'utf8');assert.ok(!/AIza[\w-]{30,}/.test(content),file);assert.ok(!/-----BEGIN (?:RSA |EC )?PRIVATE KEY-----/.test(content),file);}});
test('UID限定ルールと未設定時の拒否を維持',()=>{const rules=readFileSync(new URL('../firestore.rules',import.meta.url),'utf8');assert.match(rules,/request.auth.uid == 'REPLACE_WITH_OWNER_UID'/);assert.match(rules,/allow read, write: if false/);assert.ok(!/allow list|allow delete|if true/.test(rules));});
test('SDKセッション保持、パスワード保存・管理鍵のデプロイなし',()=>{const code=readFileSync(new URL('../dist/cloud.js',import.meta.url),'utf8');assert.match(code,/indexedDBLocalPersistence/);assert.match(code,/authStateReady/);assert.match(code,/onAuthStateChanged/);assert.ok(!/createUserWithEmailAndPassword|console\.|setItem\([^\n]*password/.test(code));assert.ok(!existsSync(new URL('../.github/workflows/backend.yml',import.meta.url)));});
test('ログインフォームにパスワード管理用の名前・属性を付ける',()=>{const code=readFileSync(new URL('../dist/cloud.js',import.meta.url),'utf8');assert.match(code,/input.name=field.name/);assert.match(code,/input.id=/);assert.match(code,/form.method='post'/);assert.match(code,/autocomplete:'username'/);assert.match(code,/autocomplete:'current-password'/);assert.match(code,/credentials.finish\(\)/);});
